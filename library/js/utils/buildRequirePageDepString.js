define([
	'underscore',
	'common',
	'jquery',
	'utils/getCurrentPageClass',

], function (_, _c, $, current_page_class){

	var build_require_dep_string = function (){
		
		
	}

	return build_require_dep_string;
	
});